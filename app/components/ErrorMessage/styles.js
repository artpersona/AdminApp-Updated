import { StyleSheet } from "react-native";

import { Colors, Fonts } from '../../config';

export default StyleSheet.create({
  errorMessage: {
    color: Colors.error,
    alignSelf: 'center',
    marginVertical: 8
  }
})